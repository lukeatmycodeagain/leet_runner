#pragma once

template <typename T>
struct Node
{
    Node *node;
    Node *parent;
    T data;
};

class LinkedList
{
private:
    /* data */
public:
    LinkedList(/* args */);
    ~LinkedList();
};

LinkedList::LinkedList(/* args */)
{
}

LinkedList::~LinkedList()
{
}
